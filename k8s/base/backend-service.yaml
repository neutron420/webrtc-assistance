apiVersion: v1
kind: Service
metadata:
  name: interview-backend
  namespace: ai-interview
spec:
  selector:
    app: interview-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
