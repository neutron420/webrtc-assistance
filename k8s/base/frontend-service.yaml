apiVersion: v1
kind: Service
metadata:
  name: interview-frontend
  namespace: ai-interview
spec:
  selector:
    app: interview-frontend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
