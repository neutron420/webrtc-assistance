# name: CI

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#   workflow_dispatch:

# permissions:
#   contents: read
#   id-token: write
#   packages: write

# env:
#   AWS_REGION: ${{ vars.AWS_REGION }}
#   ECR_BACKEND_REPOSITORY: webrtc-assistance/backend
#   ECR_FRONTEND_REPOSITORY: webrtc-assistance/frontend

# jobs:
#   backend-check:
#     name: Backend Checks
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Install backend dependencies
#         working-directory: interview-backend
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt

#       - name: Validate backend imports and syntax
#         working-directory: interview-backend
#         run: python -m compileall .

#   frontend-check:
#     name: Frontend Checks
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Bun
#         uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: latest

#       - name: Install frontend dependencies
#         working-directory: interview-frontend
#         run: bun install --frozen-lockfile

#       - name: Build frontend
#         working-directory: interview-frontend
#         env:
#           NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
#         run: bun run build

#   docker-images:
#     name: Build and Push Docker Images
#     needs:
#       - backend-check
#       - frontend-check
#     runs-on: ubuntu-latest
#     if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Generate image metadata
#         id: meta
#         run: |
#           echo "sha_tag=${GITHUB_SHA::12}" >> $GITHUB_OUTPUT

#       - name: Setup Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Build and push backend image
#         uses: docker/build-push-action@v6
#         with:
#           context: ./interview-backend
#           push: true
#           tags: |
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_BACKEND_REPOSITORY }}:${{ steps.meta.outputs.sha_tag }}
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_BACKEND_REPOSITORY }}:latest

#       - name: Build and push frontend image
#         uses: docker/build-push-action@v6
#         with:
#           context: ./interview-frontend
#           push: true
#           build-args: |
#             NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_API_URL }}
#           tags: |
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_FRONTEND_REPOSITORY }}:${{ steps.meta.outputs.sha_tag }}
#             ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_FRONTEND_REPOSITORY }}:latest
