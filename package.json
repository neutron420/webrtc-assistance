{
  "name": "webrtc-assistance-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "cd interview-frontend && bun run dev",
    "backend": "cd interview-backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "install-all": "bun install --cwd interview-frontend && cd interview-backend && pip install -r requirements.txt"
  }
}
